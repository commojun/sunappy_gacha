version: "3"
services:
  gacha-cron:
    image: commojun/cron:latest
    restart: always
    environment:
      CRONTAB_FILE: /crontab
    volumes:
      - $PWD/cron/crontab:/crontab
      - /root/suna/:/root/suna/
    logging:
      options:
        max-size: "10m"
        max-file: "3"
